# Local overrides for zsh
export TERM=xterm-256color

autoload -U +X bashcompinit && bashcompinit

eval "$(direnv hook zsh)"
export PASSWORD_STORE_GPG_OPTS="--default-key gpg-pass@archlinux.localdomain"
