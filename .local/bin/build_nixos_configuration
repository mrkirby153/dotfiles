#!/bin/bash

[ $# -ne 2 ] && echo "Usage: $0 <flake location> <host>" && exit 1

flake_url="$1"
host="$2"
derivation="nixosConfigurations.$host.config.system.build.toplevel"

echo "Building nixos host $host with flake $flake_url"

nix build "$flake_url#$derivation"
